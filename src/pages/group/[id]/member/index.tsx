import { GroupMember } from "@/containers";
import { GroupDetail } from "@/containers";

const Member = () => {
  return (
    <GroupDetail>
      <GroupMember />
    </GroupDetail>
  );
};

export default Member;
